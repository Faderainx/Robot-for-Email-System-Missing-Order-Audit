# 阶段一（邮件筛选 + 字段提取）判断与过滤逻辑 — 现行源码口径

> 对应代码：
> - `modules/mail_filter.py`（F1 规则过滤 + F2 LLM 二次意图识别）
> - `modules/field_extractor.py`（字段提取 + 置信度）
> - `modules/project_normalizer.py`（项目名标准化）
> - `modules/llm_intent.py`（意图分类 / 语义校验提示词）
> - `modules/excel_writer.py` 第 288–360 行（过滤清单着色）
>
> 口径日期：2026-09-14

---

## 一、两段式流程

```
邮件 → F1 规则过滤 → valid_mails（进入提取）
                   → filtered_mails（进"过滤清单"）
        ↓
F2 LLM 二次识别（只对 filtered_mails 中 llm_eligible != False 的邮件做意图分类）
```

**F1 过滤掉的两类不送 LLM**（省钱 + 不污染人工复核台）：
① 非注册业务（收款/报价/信息变更/发票）② 证书/下号通知。这两类会把 `llm_eligible` 置为 `False`。

---

## 二、关键词表（逐字，共 8 组）

### 1. `KEYWORDS_ACTION` — 注册动作词
`注册` `新增` `撤单` `注销` `追加`

### 2. `KEYWORDS_REQUEST` — 一般请求词（"要办理"的宽松写法）
`申请` `办理` `提交` `加入` `开通` `新注册` `注册申请` `申请表`
`application` `register` `registration` `onboarding`

### 3. `KEYWORDS_EXPLICIT_REQUEST` — 明确"现在要办"的词
`申请` `办理` `提交` `加入` `开通` `新注册` `注册申请` `申请表` `application` `apply` `onboarding`

> 与第 2 组的差别：**去掉了 `register` / `registration`**。目的是不让
> `registration completed` / `registration certificate` 这类完成态通知被当成新申请
> （它们仍可用于普通业务命中，但不能阻止"证书通知"过滤）。

### 4. `KEYWORDS_ATTACHMENT_REQUEST` — 附件名请求词
`申请表` `application form` `registration form` `onboarding form`
（只扫**附件名+附件正文**，因为正文可能解析失败，附件名常是唯一线索）

### 5. `KEYWORDS_BUSINESS` — 业务词（项目/国家名）
`WEEE` `weee` `电池法` `电池` `包装法` `EPR` `epr` `一次性塑料` `BAT` `bat`
`德国` `法国` `意大利` `西班牙` `荷兰` `波兰` `瑞典` `比利时` `爱尔兰` `葡萄牙` `奥地利`
`丹麦` `捷克` `芬兰` `挪威` `匈牙利` `罗马尼亚` `保加利亚` `希腊` `克罗地亚` `卢森堡`
`爱沙尼亚` `瑞士` `英国` `加拿大` `拉脱维亚` `包装` `packaging` `verpackg` `ppwr` `battery` `batteries`

### 6. `KEYWORDS_IRRELEVANT` — 无关词（须**无**业务词、**无**动作词才过滤）
`账单` `invoice` `合同` `协议` `咨询` `反馈` `建议` `下证` `证书号` `证书下发` `保证金` `担保` `退款` `回收费` `购买`

### 7. 非注册业务词（成交后运营事务）

**`KEYWORDS_NON_REGISTRATION_STRICT`（命中即过滤，哪怕主题带"申请/办理"）**
- 收款/水单：`收款` `已收款` `付款` `回款` `汇款` `转账` `水单` `到账` `对账` `bank slip` `remittance` `payment received` `payment`
- 报价/续费：`报价` `报价单` `询价` `价格` `年费` `续费` `续期` `服务费` `收费标准` `quotation` `quote` `renewal`
- 信息变更：`修改公司名称` `公司名称修改` `公司名称变更` `变更公司名称` `修改公司名` `改公司名` `公司更名` `更名` `修改地址` `地址修改` `地址变更` `变更地址` `修改公司信息` `公司信息变更` `信息变更` `资料变更`
- 发票账单：`发票` `开票` `开发票` `账单` `对账单` `invoice`

**公司改名正文识别**：对正文前 5000 个字符额外识别 `公司名称变更`、`公司更名`、
`公司名称更新`、`公司名称改为`、`主体变更`、`company name change` 等明确表达。
正文命中时先进入意图 Agent 复核；未启用 LLM 时直接过滤，避免误进入询单队列。

**`KEYWORDS_NON_REGISTRATION_SOFT`（仅当主题**没有**注册动作词时才过滤）**
`授权书` `授权函` `待签字` `签署` `签字` `盖章` `公证` `power of attorney`

> 设计意图：`新增德国WEEE注册(附授权书)` 里的"授权书"只是配套材料，不能因此丢掉注册需求。

### 8. 证书/下号通知词

**`KEYWORDS_CERTIFICATE_NOTICE`**（扫主题 + 正文）
`证书` `下证` `下号` `注册号下发` `certificate` `certificates` `certification` `registration number`

**`KEYWORDS_CERTIFICATE_COMPLETION`**（完成态）
`已完成` `注册完成` `完成注册` `注册已完成` `已下证` `证书已下发` `证书下发` `已下发`
`请查收附件` `请查收证书` `证书请查收` `请妥善保存` `completed` `certificate attached` `attached certificate`

---

## 三、F1 判定顺序（严格按此顺序，命中即 continue）

| 序 | 条件 | filter_status | 过滤原因文案 |
|---|---|---|---|
| 0 | `skip_reason == "self_sent"` | （直接进 filtered） | `自身发送` |
| 1 | 主题命中 **STRICT** 词，或命中 **SOFT** 词且主题无动作/请求词 | `filtered` | `非注册业务({关键词})`，`llm_eligible=False` |
| 2 | 有证书通知词 **且**（有完成态词且无明确请求词 **或** 无任何动作/请求词） | `filtered` | `证书/下号通知，非新增申请`，`llm_eligible=False` |
| 3 | 有无关词 且 无业务词 且 无动作词 | `filtered` | `内部无关邮件` / `外部无关邮件` |
| 4 | 有动作/请求词 **且** 有业务词 | **`valid`** | `方法一: 注册关键字+业务词命中` |
| 5 | 有动作/请求词 **且** 主题含 `KEYWORDS_ACTION` 或 `KEYWORDS_REQUEST` | **`valid`** | `方法一扩展: 主题含注册关键字` |
| 6 | 只有业务词 | **`uncertain`** | `业务关键字命中但未见注册动作，需人工复核`（**仍进 valid_mails**） |
| 6b | 只有业务词 且 有无关词 且 无动作词 | `filtered` | `内部/外部无关邮件(业务词+无关词混存)` |
| 7 | 内部邮箱 且 无业务词 且 无动作词 | `filtered` | `内部无关邮件` |
| 8 | 无业务词 且 无动作词 | `filtered` | `外部无关邮件` |
| 9 | 以上都不命中 | **`uncertain`** | `不确定, 需人工复核`（**保留**） |

**扫描范围的三条边界（很重要）**
- 第 1 组（非注册业务）**只扫主题**——正文/签名档/报价附件里出现"价格""发票""payment"太常见，扫全文会误杀真实注册邮件。
- 证书通知词**扫主题+正文**。
- 业务词**扫主题+正文+附件名+附件正文**。
- 附件请求词**只扫附件名+附件正文**。

输出日志：`规则过滤完成: 有效 N 封, 过滤 M 封`。

---

## 四、`filter_status` / `filter_reason` 关键词清单

### `filter_status` 只有 3 种取值
| 关键词 | 含义 |
|---|---|
| `valid` | 有效，进入字段提取 + 工单查询 |
| `uncertain` | 不确定，**保留在 valid_mails**，但转人工复核 |
| `filtered` | 过滤掉，进"过滤清单" |

### `过滤清单` 里的 `filter_reason` 文案（含 `uncertain` 是灰色的判定）
`自身发送` / `非注册业务({关键词})` / `证书/下号通知，非新增申请` / `内部无关邮件` /
`外部无关邮件` / `内部无关邮件(业务词+无关词混存)` / `外部无关邮件(业务词+无关词混存)` /
`业务关键字命中但未见注册动作，需人工复核` / `不确定, 需人工复核` /
`... | LLM失败` / `LLM恢复(意图=..., 原因=...)` / `... | LLM确认={类型}({原因})`

### 下游着色（`excel_writer.py`，改文案会失效）
| 条件 | 效果 |
|---|---|
| `filter_status == "uncertain"` 且有 `filter_reason` | 灰行 |
| `置信度 == "low"` 或 `代理匹配方式` 含 `待确认` | 蓝（第 5/7/9 列） |
| `语义校验状态 ∈ {uncertain, invalid, LLM调用失败/未完成}` | 写入"人工复核提示" |
| `置信度 == "需人工确认"` | 写入"人工复核提示" |

---

## 五、置信度（`置信度` 列）— 4 种取值

`field_extractor._calc_confidence()`：代理 / 客户 / 项目**各命中一个 +1 分**

| 得分 | 置信度 |
|---|---|
| 3 分 | `high` |
| 2 分 | `medium` |
| ≤1 分 | `low` |
| 代理匹配方式以 `多匹配` 开头 | **直接 `low`** |

**两个覆盖优先的取值**（优先级高于上面的分数）：
- `需人工确认`：LLM 判定 `need_review=True` **或** 该邮件 `filter_status == "uncertain"`
- `low`（OCR 命中时）：用了 OCR 兜底就降为 `low`（分数法被跳过）

> 注意：`需人工确认` 不是 high/medium/low 三档里的值，是**独立的第 4 个取值**。

---

## 六、语义校验状态（LLM 批量校验回填）— 4 种

| 关键词 | 来源 |
|---|---|
| `valid` | LLM 判定字段无问题 |
| `uncertain` | LLM 判定不确定 |
| `invalid` | LLM 判定有问题 |
| `LLM调用失败/未完成` | 调用异常或没跑完 |

附列：`语义问题字段` / `语义校验建议` / `语义校验原因` / `语义校验模型`。

---

## 七、LLM 意图分类（F2 二次识别）— 7 类

`注册类` / `账单` / `合同` / `咨询` / `下证` / `购买` / `其他`

判定关键词（写死在提示词里）：
- 注册类：`注册` `新增` `撤单` + 环保合规项目（`WEEE` `电池法` `包装法` `EPR` 等）
- 账单：`账单` `invoice` `费用`
- 合同：`合同` `协议`
- 咨询：`咨询` `问题` `反馈`
- 下证：`下证` `证书` `下号`
- 购买：`购买` `担保` `回收费`

只有判定为 **注册类** 才会把 `filter_status` 改回 `valid`，并写 `LLM恢复(意图=..., 原因=...)`。

LLM 提取字段时返回的 `confidence` 是 `high|medium|low`，提取不到就 `medium`；无法确定归属的项目/公司一律降为 `low`，**不静默漏掉**。
`需求` 字段只能是：`注册` / `新增` / `撤单`（判断不了给空字符串）。

---

## 八、项目名标准化的关键词（`project_normalizer.py`）

**国家（24 个）**
`德国` `荷兰` `爱尔兰` `意大利` `比利时` `波兰` `丹麦` `法国` `捷克` `葡萄牙` `瑞典` `西班牙`
`卢森堡` `奥地利` `匈牙利` `芬兰` `罗马尼亚` `挪威` `爱沙尼亚` `瑞士` `希腊` `英国` `加拿大` `拉脱维亚`

**业务类型（4 种）**：`WEEE` `电池法` `包装法` `一次性塑料`

**标准格式**：`国家 + 项目类型`（如 `德国WEEE`、`法国电池法`、`波兰包装法`）。
**组合项目必须拆开**：`德国WEEE+电池法` → `["德国WEEE", "德国电池法"]`。
`COMBO_MAP` 里另有 13 条固定组合映射（荷兰/爱尔兰/意大利/波兰/捷克/匈牙利/挪威 的 WEEE+电池法+包装法 组合）。

---

## 九、工作台（`workbench_server.py`）的状态判定

`review`（需人工）的触发条件，**满足任一**：
- `_source == "人工补全"`
- `人工复核提示` 或 `待人工补全` 非空
- `语义校验状态 ∈ {uncertain, invalid, llm调用失败/未完成}`
- `置信度 ∈ {low, medium}`
- `代理` 为空

否则 → `ready`。
