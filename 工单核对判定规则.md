# 工单核对（阶段二）判定规则 — 现行源码口径

> 对应代码：`modules/workorder_checker.py`
> - `match_records()` 第 2011–2142 行（判定主逻辑）
> - `search_one()` 第 1495–1563 行（查询 + 模糊重查）
> - `_get_fuzzy_company_terms()` / `_strip_admin_prefix()`（模糊关键词生成）
> - `modules/excel_writer.py` 第 592–611 / 620–652 行（着色规则）
>
> 口径日期：2026-09-14

---

## 一、主判定规则（只看"页面精确查询返回几条"）

查询条件本身就是"公司 + 所属代理 + 服务项目 + 国家（+日期窗口）"的精确条件，
所以**平台返回的条数就是最终答案**，本地字段比对只做提示，不推翻结论。

| 页面返回 | 是否已录单 | 匹配状态（关键词） | Excel 呈现 |
|---|---|---|---|
| **1 条** | `是` | `精确匹配` | 白（正常） |
| **1 条**，但本地代理/客户/项目对不上 | `是` | `已录单-字段不一致需复核` | 白，矛盾写在"日期筛选说明" |
| **1 条**，但平台下单日期超出邮件日期窗口 | `是` | `已录单-日期异常需复核` | **黄** |
| **≥2 条** | `待复核` | `多匹配-人工排查` | **橙**（匹配状态单元格） |
| **0 条**（含模糊重查后仍 0 条） | `否` | `漏单` | **整行红** |

一句话给同事：**1 条=已录单，≥2 条=待复核，0 条=先模糊重查公司名，仍 0 条才算漏单。**

---

## 二、查询流程（"返回条数"是怎么来的）

1. 打开工单系统，填 5 个条件：**公司名称 + 所属代理 + 服务项目 + 国家 + 日期窗口** → 点查询。
2. 返回 **≥1 条** → 直接交给判定（不再改条件）。
3. 返回 **0 条** → **只清空"公司名称"输入框**（代理/项目/国家/日期都不动），换成模糊关键词重查，命中即停。
4. 模糊重查仍 **0 条** → 判 `漏单`。
5. **结果表读不出来**（解析失败）→ 不判漏单，整条标 `未比对`，且**不写缓存**。

> 注意：只有"精确 + 模糊都为 0"才是漏单。读到 0 条但结果表解析失败时**不算漏单**。

---

## 三、模糊重查用的关键词（逐字规则）

`_get_fuzzy_company_terms(公司名)` 只生成安全变体：繁简转换、去空白、去主体类型说明括号、开头的行政区划**逐级单独删**。
候选按优先级排序，逐个查、**命中即停**。

| 顺序 | 规则 | 例 A：`深圳市宝安区宝安某某有限公司` | 例 B：`杭州市余杭区XX电子商务有限公司（个体工商户）` |
|---|---|---|---|
| 1 | **繁体转简体**（只改变字形，不删实体） | 原样 | 若为繁体名，补简体写法 |
| 2 | **去空白格式变体**（公司后缀保留） | 无空白则不变 | 英文名可补无空格写法 |
| 3 | **去掉主体类型说明括号**（只删说明类，地名/国别括号保留——见下方专段） | 无括号，不变 | `杭州市余杭区XX电子商务有限公司` |
| 4 | **删第一级区划**（省/市） | `宝安区宝安某某有限公司` | `余杭区XX电子商务有限公司` |
| 5 | **删第二级区划**（区/县） | `深圳市宝安某某有限公司` | `杭州市XX电子商务有限公司` |
| 6 | **全删区划前缀** | `宝安某某有限公司` | `XX电子商务有限公司` |
| 7 | 原始全名（**最后一位，且实际跳过不再查**，因为精确查询已查过） | — | — |

> 例 A 依次会查 3 个词，例 B 依次会查 4 个词，任何一个命中就停。
> 候选必须满足：与原全名不同、长度 ≥ 2 字、不与前面重复，否则不入列。

**每一刀切完都必须还留有主体名**，否则这一刀作废：

- `深圳市宝安区有限公司` 删市级会得到 `宝安区有限公司` —— 主体名被剃光、只剩区名接后缀，
  这种词搜出来是废词，直接跳过（不是"查了没有"，是"根本不查"）。
- 同一条规则也挡掉了 `某某夜市有限公司` → `有限公司`、`城市之家有限公司` → `之家有限公司`
  这类把品牌词当行政区划切的误伤。

业务上最典型的两条：

- `xx市xx区xx有限公司` → 先删市级查 **`xx区xx有限公司`**，再删区级查 **`xx市xx有限公司`**，最后查 **`xx有限公司`**
- `xx有限公司（个体工商户）` → 去掉类型说明括号 → 查 **`xx有限公司`**

### 括号：只删「主体类型说明」，地名/国别括号一律保留

中英文括号 `()（）【】[]` 都识别，但**只有括号内容属于主体类型说明时才整块删掉**。
白名单词（括号内容含其中任一即视为说明）：`个体` `独资` `合伙` `分支` `分公司` `一人`
`小微` `农民专业` `外商投资` `中外合资` `中外合作` `台港澳` `自然人` `集团`；空括号也照删。

| 名单里的写法 | 是否删括号 | 实际会查 |
|---|---|---|
| `xx有限公司（个体工商户）` | 删 | `xx有限公司` |
| `某某（自然人独资）有限公司` | 删 | `某某有限公司` |
| `深圳某某（上海）贸易有限公司` | **不删** | 原名不动，**零候选** |
| `某某（中国）有限公司` | **不删** | 原名不动，**零候选** |
| `xx有限公司（）` | 删 | `xx有限公司` |

> **为什么不一律删**：`某某（中国）有限公司` 删掉括号会变成 `某某有限公司`，
> 而平台上往往真有一家叫 `某某有限公司` 的**别家**主体，一查就中 → 判"已录单" → 漏检。
> 括号里是地名/国别时，它是区分实体的标识，不是补充说明。
> 之所以取白名单（而不是"凡是地名就不删"的黑名单）：**漏删只是少查一轮，属保守失败；
> 误删会拼出别家公司名，制造假"已录单"，属危险失败**，两害相权取其轻。

**公司后缀必须保留，不得删除。** `有限公司 / 有限责任公司 / 股份有限公司 / 股份公司 / 集团 / 公司`
一律不删，也不做"取前 N 个字"的截短。原因：后缀是公司主体名的边界，
删掉后（如 `青岛积米厨房用品有限公司` → `青岛积米`）会命中一大批同名前缀的无关公司，
把"精确未命中"变成"模糊乱命中"，反而制造错误的"已录单"结论。

> 若去掉类型说明括号、区划和安全格式变体后与原全名完全相同（如 `上海某某科技有限公司`、
> 括号里是地名的 `深圳某某（上海）贸易有限公司`），**不产生任何候选**。
> 繁体公司名（如 `保文商貿有限公司`）会补一条简体候选 `保文商贸有限公司`；
> 仍不会删除后缀或只取前 N 个字。

**行政区划只剥名字开头、且逐级剥离**：`广东省广州市天河区某某网络科技有限公司` →
先剥 `广东省` → 再剥 `广州市` → 再剥 `天河区` → 得到 `某某网络科技有限公司`。

三条防误伤的闸门（避免把品牌词当行政区划切掉）：

1. 区划词前面的地名**至少 2 个字**才剥 → `城市之家有限公司` 不会被切成 `之家有限公司`；
2. 剥完**只剩公司后缀**就放弃这一刀 → `某某夜市有限公司` 不会被切成 `有限公司`；
3. 剥完剩余**不足 4 字**就放弃 → 不把主体名剃成两三个字（那种词搜出来全是无关公司）。

前置条件：公司名长度 > 2 字才触发模糊重查；**只清空"公司名称"输入框**，代理/项目/国家/日期一律不动。
日志会打印候选清单（`模糊匹配候选词(N): A → B → C`）和最终命中的那个词；结果表新增
`查询方式`、`模糊查询词`两列，分别记录“精确查询 / 模糊匹配 / 缓存复用”和实际命中的公司词；
无候选时打 `公司名去掉类型说明括号与行政区划后与原名相同，无可用的模糊候选词`。

---

## 四、关键词清单（下游要按这些字符串精确比对）

### 1. `是否已录单`（结果表第 10 列）— 共 5 种

| 关键词 | 含义 |
|---|---|
| `是` | 已录单（返回 1 条） |
| `否` | 漏单（精确 + 模糊都为 0 条） |
| `待复核` | 返回 ≥2 条，需人工挑 |
| `未查询` | 前置校验跳过，没查 |
| `未比对` | 查询过程失败（含结果表解析失败），**不是业务结论** |

### 2. `匹配状态`（结果表第 13 列）— 共 13 种

**正常判定产生 5 种：**
`精确匹配` / `已录单-字段不一致需复核` / `已录单-日期异常需复核` / `多匹配-人工排查` / `漏单`

**异常 / 跳过产生 8 种：**
`客户名称无效-跳过查询` / `前置校验跳过` / `未执行` / `查询未执行` /
`查询条件填写失败` / `查询按钮失败` / `查询异常` / `结果表解析失败`

### 3. `RPA查询状态`（结果表第 14 列）— 共 12 种

`已查询`（默认） / `实时查询` / `缓存复用` / `缓存复用-已展示` / `缓存复用-展示失败` /
`跳过` / `未执行` / `查询未执行` / `查询条件填写失败` / `查询按钮失败` / `查询异常` / `结果表解析失败`

### 3.5 `工单日期` 与 `下单日期` — 两个不同的日期，别搞混（关键）

这两列**不同源**，业务含义是"客户先发邮件 → 我们再到工单系统下单"这条时间链：

| 列 | 含义 | 来源 |
|---|---|---|
| `工单日期` | **邮件发来的日期**（发件日期） | 阶段一从邮箱读到的邮件发件日期，与本表"发件日期"列同源 |
| `下单日期` | **下单日期** | 工单系统查询结果里返回的那一列，取**页面原样文本** |

| 文件 | 列位置 |
|---|---|
| `workorder_check_result.xlsx`（工单核对，阶段二结果） | `工单日期` = 第 **11** 列，`下单日期` = 第 **12** 列 |
| `漏单清单_*.xlsx`（一站式主产物） | `工单日期` = 第 **13** 列，`下单日期` = 第 **14** 列 |

取值规则：

- `工单日期` → 恒等于该封邮件的发件日期，任何状态都有值
- `下单日期` → 已录单 / 待复核时写命中工单的下单日期；返回 ≥2 条时取**日期最近**那条；
  漏单、未查询、未比对 → 留空
- `下单日期` 保留平台原文（如 `2026-08-20`），**不做格式化、不加时分秒**，
  方便与工单系统列表里那一列**逐字对照**

> 日期窗口比对（`已录单-日期异常需复核`）用的是**平台下单日期**与邮件日期的差值，
> 与 `工单日期` 无关——`工单日期` 就是邮件日期，自己跟自己比没有意义。

### 4. 下游靠"字符串包含/相等"识别的关键词（改文案会失效）

| 判断处 | 关键词 | 效果 |
|---|---|---|
| 整行红 | `是否已录单 == "否"` | 漏单 |
| 灰行 | `RPA查询状态 == "跳过"` 或 `== "未执行"` | 未跑/被跳过 |
| 黄行 | 匹配状态**包含** `日期异常` | 日期超窗口 |
| 橙格 | 匹配状态**包含** `多匹配` | 多候选待排查 |
| 漏单复查清单 | 匹配方式**包含** `多匹配` 或 命中 0 条 | 进入人工复查 |

---

## 五、本地比对参数（只作提示，不改结论）

配置在 `config.yaml` 的 `workorder_match` 段：

| 维度 | 规则 | 阈值 |
|---|---|---|
| 客户 | 模糊匹配 | `customer_threshold: 85`（默认 85） |
| 代理 | 模糊匹配，或互为子串 | 内部固定 80；**任一侧为空 → 视为通过**（只标注"代理校验：代理为空-未按代理筛选"） |
| 项目 | 模糊匹配；任一侧为空 → 不通过 | 内部固定 85 |
| 日期 | 邮件发件日**前后 1 个日历月** | `date_window_months: 1`；无窗口时退回 `date_tolerance_days: 60` |

> 代理为空**不会**判成漏单，只是该维度无法校验。

### `fuzzy_match_pair(名单值, 工单值, 阈值)` 内部怎么算

实现在 `utils/fuzzy_match.py`，**四道关，任一通过即算一致**：

| 顺序 | 判定 | 说明 |
|---|---|---|
| 0 | 归一化 | 繁→简（OpenCC）、去所有空白、全角括号转半角、`，`→`,` `。`→`.`、转小写 |
| 1 | **互为子串** | `A in B` 或 `B in A` → **直接通过**（不看分数）。例：`璟深` 在 `深圳市璟深电子商务有限责任公司` 内 |
| 2 | 整体相似度 | `fuzz.ratio ≥ 阈值` → 通过 |
| 3 | 局部相似度 | 两侧长度都 ≥ 2 且 `fuzz.partial_ratio ≥ 90` → 通过（**固定 90，与传入阈值无关**） |
| — | 其余 | 不通过 |

> 注意第 1 关是无条件通过：名字短到成了别人的子串就会算一致（如名单写 `璟深`、工单写 `深圳市璟深电子商务有限责任公司`）。
> 这是**刻意的**——业务上名单常写简称、工单常写全称。但反过来也意味着：`科技` 这种词只要出现在工单名里就算一致，所以客户维度不要传过短的词进去。

---

## 六、三条防误判保护

1. **结果表解析失败 → 标 `未比对`，绝不判漏单，也不写缓存**（防止故障被写成"0 条"并向后续批次扩散）。
2. **连续 3 条查询失败**（`max_consecutive_query_failures: 3`）→ **终止本批**；批次里没跑到的行标 `未执行` / `未查询`。
3. **本批 ≥5 条且全部返回 0 条** → 日志打警告，提醒核对查询条件是否真的生效。

---

## 七、"漏单"这个词的准确含义

只有 **精确查询 0 条 → 模糊重查也 0 条** 才是 `漏单`。
任何读取/填写/点击层面的故障，一律走 `未比对` 或 `待复核`，**不占用"漏单"这个词**。
所以看到 `否 / 漏单` 可以当作业务结论直接使用。
